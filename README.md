# Daklapack Tech Assessment
## Running the project locally:
Navigate to project folder that contains .csproj file and execute:
- `dotnet build`

- `dotnet run`

The Swagger UI will be accessible at https://localhost:5079/swagger
